#include <iostream>
#include <vector>


// DP 인거같음, 풀이 안보면 노답이다

int main()
{
	std::ios_base::sync_with_stdio(false);
	std::cin.tie(NULL);
	std::cout.tie(NULL);

	int N, K;
	std::cin >> N >> K;



	return 0;
}